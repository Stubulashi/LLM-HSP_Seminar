# IPLE 核心代码发布版 — 忽略规则（安全网）
# 实验数据与输出（不入库）
data/
results/
logs/
cloud_backup/
datasets/

# 环境与缓存
.venv/
__pycache__/
*.pyc
.pytest_cache/
.cache/

# 模型权重（本地缓存不提交）
*.safetensors
*.bin

# 密钥与本地环境（绝不提交）
.env
.env.*
!*.env.example

# 大体积归档与外链交付物（见根目录 ASSETS_INDEX.md 第 7 节）
*.tar.gz
*.zip
*.pdf
