# IPLE dependency list (see the supplementary conventions table in docs/decisions.md)
# Missing basis: scaf.md L132-136 only lists "CHECK required_packages"; versions are initial values, confirmed at the first PR review
torch>=2.2
transformers>=4.40
vllm>=0.5.0  # AutoDL cost optimisation: the vllm backend speeds up 14B/32B runs (tight constraint on the torch version, see the planning assumptions)
accelerate>=0.30
bitsandbytes>=0.43
pyyaml>=6.0
pandas>=2.0
statsmodels>=0.14
sentence-transformers>=2.6
pytest>=8.0
tqdm>=4.66
typer>=0.12
